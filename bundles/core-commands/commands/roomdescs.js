'use strict';

exports.command = (rooms, items, players, npcs, Commands) => {
  return (args, player) => {
    // TODO: Refactor this into a `toggle` command
    // player.setPreference('roomdescs', option);
    return;
  }
};
