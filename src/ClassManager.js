'use strict';

/**
 * Maining list of classes in the game
 * @see PlayerClass
 * @extends Map
 */
class ClassManager extends Map {
  /*
   * Classes right now have no general "manager" like needs besides having a
   * list of classes but this class is used regardless in case that need arises
   * in the future.
   */
}

module.exports = ClassManager;
